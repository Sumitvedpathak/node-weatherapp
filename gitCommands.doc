Git Bash
Create SSH
ls -a -l ~/.ssh
ssh-keygen -t rsa -b 4096 -C "Comment/email address"
eval $(ssh-agent -s) -- starts ssh agent
ssh-add -K ~/.
cat ~/.ssh/id_rsa.pub --to get public key
ssh -T git@github.com -- to test authenticated with Github

--To push the code to git first time
git push origin HEAD:main -- to push code to github

-- to add changes to git
git status
git stage .
git commit -m ""
git push


-- to see all our remotes connected 
git remote


Heroku setup
heroku login
heroku keys:add --adds ssh keys to
heroku create sumitvedpathak-weather-app --creates an web app in huroku
git push heroku main -- pushes the git code to heroku to above set domain

